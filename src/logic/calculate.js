export default function calculate(calculatorData, buttonName) {
  let total;
  let next;
  const operation = '';

  if (buttonName === '+/-') {
    total *= -1;
    next *= -1;
  }

  return calculatorData;
}
